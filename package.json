{
  "name": "si-gesit",
  "version": "1.0.0",
  "private": true,
  "description": "SI-GESIT — Pengaduan Masyarakat Kemenag Barito Utara (dev orchestrator)",
  "scripts": {
    "dev": "concurrently -k -n BE,FE -c yellow,cyan \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && go run github.com/air-verse/air@v1.67.4 -c .air.toml",
    "dev:frontend": "cd frontend && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
